{
  "scripts": {
    "alex": "alex .",
    "lint": "npm-run-all prettier alex markdownlint",
    "lint:dev": "npm-run-all prettier:dev alex markdownlint",
    "markdownlint": "markdownlint '**/*.md' --ignore node_modules --ignore '**/CHANGELOG.md' --ignore '**/LICENSE.md'",
    "prettier": "prettier --check .",
    "prettier:dev": "prettier --write ."
  },
  "devDependencies": {
    "@relucent.dev/prettier-config": "^2.2.1",
    "alex": "^11.0.1",
    "markdownlint-cli": "^0.40.0",
    "npm-run-all": "^4.1.5",
    "prettier": "^3.3.3"
  }
}
